<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
    <style>
        /* Global Styles */

        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #3498db, #9b59b6);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Container Styles */

        .container {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            width: 400px;
            text-align: center;
        }

        h2 {
            color: #333;
        }

        /* Form Styles */

        form {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            display: flex;
            align-items: center;
            width: 100%;
            margin: 15px;
        }

        label span {
            width: 30%;
            text-align: right;
            margin-right: 15px;
            color: #555;
            font-size: 16px;
        }

        input {
            width: 70%;
            padding: 12px;
            box-sizing: border-box;
            margin: 10;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        /* Button Styles */

        .btn {
            display: inline-block;
            padding: 12px 24px;
            margin-top: 20px;
            background-color: #2ecc71;
            color: #fff;
            text-align: center;
            text-decoration: none;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
            width: 50%;
            box-sizing: border-box;
        }

        .btn:hover {
            background-color: black;
        }

        /* View Survey Data Button */

        .btn-view {
            background-color: #3498db;
        }

        /* Logout Button */

        .btn-logout {
            background-color: #e74c3c;
        }

        /* Additional Styling for Improved Look and Feel *
        /* Adjusted background color for the "View Survey Data" and "Logout" buttons */

        .btn-view {
            background-color: #2980b9;
        }

        .btn-logout {
            background-color: #c0392b;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Admin Panel</h2>
        <form action="/admin" method="post">
            <label for="question">
                <span>Question:</span>
                <input type="text" id="question" name="question" required>
            </label>

            <label for="options">
                <span>Options (comma-separated):</span>
                <input type="text" id="options" name="options" required>
            </label>
            <div>
                <label for="type"><span>Type:-</span>
                    <label for="checkbox"> Checkbox
                        <input type="radio" id="checkbox" name="type" value="checkbox">
                    </label>
                    <label for="radio">Radio
                        <input type="radio" id="radio" name="type" value="radio">
                    </label>
                </label>
            </div>
            <button class="btn" type="submit">Add Question</button>
        </form>

        <a href="/admin_dashboard" class="btn btn-view">View Survey Data</a>
        <a href="/" class="btn btn-logout">Logout</a>
    </div>
</body>

</html>