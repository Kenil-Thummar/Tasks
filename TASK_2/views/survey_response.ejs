<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Survey Dashboard</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #3498db;
            color: #fff;
            padding: 20px;
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            color: #2c3e50;
        }

        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }

        th,
        td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #3498db;
            color: #fff;
        }

        tbody tr:hover {
            background-color: #f5f5f5;
        }

        .goback-btn {
            display: block;
            margin: 20px auto;
            padding: 12px 25px;
            /* Adjusted padding for a smaller button */
            width: 150px;
            /* Fixed width for the button */
            background-color: #2ecc71;
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-align: center;
        }

        .goback-btn:hover {
            background-color: #27ae60;
        }
    </style>
</head>

<body>
    <header>
        <h1>Survey Dashboard</h1>
    </header>

    <table>
        <thead>
            <tr>
                <th>Question</th>
                <th>User Answer</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(entry=> { %>
                <tr>
                    <td>
                        <%= entry.question %>
                    </td>
                    <td>
                        <%= entry.response %>
                    </td>
                </tr>
                <% }); %>

                    <!-- Additional rows for survey feedback -->
                    <tr>
                        <td>Survey Response</td>
                        <td>
                            <%= feedback.surveyresponse %>
                        </td>
                    </tr>
                    <tr>
                        <td>Suggestion</td>
                        <td>
                            <%= feedback.suggestion %>
                        </td>
                    </tr>
                    <tr>
                        <td>Email</td>
                        <td>
                            <%= feedback.email %>
                        </td>
                    </tr>
        </tbody>
    </table>

    <!-- Go Back Button -->
    <a href="/admin_dashboard" class="goback-btn">Go Back</a>
</body>

</html>